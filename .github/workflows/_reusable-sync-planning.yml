name: Sync Planning Minimal

"on":
  workflow_call:
    inputs:
      framework:
        type: string
        default: 'auto'
      create-project:
        type: boolean
        default: false
      sync-todos:
        type: boolean
        default: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
    steps:
      - run: echo "Test with permissions"
